#include <iostream>
#include <unistd.h>

using namespace std;

int main(int argc, char *argv[])
{
    if (0 == argc)
    {
    }
    else
    {
    while(-1 != getopt(argc, argv,"a:g");
    cout << "Какой пароль нужен?" << endl;
    }
}